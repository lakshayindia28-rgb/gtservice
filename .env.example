# Contact form email (SMTP)
#
# IMPORTANT
# - Never commit real passwords/keys.
# - Create a local/server-only `.env` from this file.

# Contact API port (Nginx should proxy /api/* to this)
CONTACT_API_PORT=8787

# SMTP settings (ask your email provider for these)
SMTP_HOST=smtp.zoho.in
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=admin@thegtservices.com
SMTP_PASS=

# If Zoho 2FA is enabled, use a Zoho "App Password" here (not your normal login password).

# Email routing
MAIL_FROM="GT Services <admin@thegtservices.com>"
MAIL_TO=info@thegtservices.com

# Used in email footer
PUBLIC_SITE_URL=https://thegtservices.com
